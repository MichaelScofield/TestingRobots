message TransUnit {
    required int32 sn = 1;
    extensions 100 to max;
}

message LogInRequest {
    required string device_id = 1;
    required string client_version = 2;
    required string meta_crc32 = 3;
}

extend TransUnit {
    optional LogInRequest msg = 1001;
}